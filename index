<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>OrientaGuide — Outil d'orientation étudiante</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
<style>
*{box-sizing:border-box;margin:0;padding:0;}
:root{
  --bg:#F0F4FF;
  --white:#FFFFFF;
  --blue:#3B5BDB;
  --blue-light:#EEF2FF;
  --blue-mid:#748FFC;
  --dark:#1C1C2E;
  --gray:#6B7280;
  --border:#E5E7EB;
  --green:#10B981;
  --green-light:#D1FAE5;
  --purple:#7C3AED;
  --purple-light:#EDE9FE;
  --orange:#F59E0B;
  --orange-light:#FEF3C7;
}
body{font-family:'Plus Jakarta Sans',sans-serif;background:var(--bg);color:var(--dark);min-height:100vh;}

/* HEADER */
header{background:var(--white);border-bottom:0.5px solid var(--border);padding:1rem 2rem;display:flex;align-items:center;justify-content:space-between;}
.logo{display:flex;align-items:center;gap:10px;}
.logo-icon{width:36px;height:36px;border-radius:10px;background:var(--blue);display:flex;align-items:center;justify-content:center;color:var(--white);font-size:16px;font-weight:800;}
.logo-name{font-size:16px;font-weight:700;letter-spacing:-0.5px;}
.logo-name span{color:var(--blue);}
.header-badge{font-size:11px;background:var(--blue-light);color:var(--blue);padding:5px 12px;border-radius:20px;font-weight:600;}

/* HERO */
.hero{text-align:center;padding:3rem 1.5rem 2rem;}
.hero-tag{display:inline-flex;align-items:center;gap:6px;background:var(--blue-light);color:var(--blue);font-size:11px;font-weight:700;padding:5px 14px;border-radius:20px;margin-bottom:1rem;text-transform:uppercase;letter-spacing:0.5px;}
.hero h1{font-size:36px;font-weight:800;letter-spacing:-1.5px;line-height:1.1;margin-bottom:0.8rem;}
.hero h1 span{color:var(--blue);}
.hero p{font-size:14px;color:var(--gray);line-height:1.7;max-width:480px;margin:0 auto;}

/* PROGRESS */
.progress-wrap{max-width:600px;margin:1.5rem auto 0;padding:0 1.5rem;}
.progress-steps{display:flex;align-items:center;gap:0;margin-bottom:0.5rem;}
.pstep{flex:1;height:4px;border-radius:2px;background:var(--border);transition:background 0.4s;}
.pstep.done{background:var(--blue);}
.pstep.active{background:var(--blue-mid);}
.pstep+.pstep{margin-left:4px;}
.progress-label{font-size:11px;color:var(--gray);text-align:center;}

/* FORM CONTAINER */
.form-wrap{max-width:600px;margin:0 auto;padding:1.5rem;}

/* STEP CARD */
.step-card{background:var(--white);border-radius:20px;padding:2rem;border:0.5px solid var(--border);display:none;animation:fadeIn 0.3s ease;}
.step-card.active{display:block;}
@keyframes fadeIn{from{opacity:0;transform:translateY(8px)}to{opacity:1;transform:translateY(0)}}

.step-eyebrow{font-size:11px;font-weight:700;color:var(--blue);text-transform:uppercase;letter-spacing:1px;margin-bottom:0.5rem;}
.step-title{font-size:20px;font-weight:700;letter-spacing:-0.5px;margin-bottom:0.3rem;}
.step-sub{font-size:13px;color:var(--gray);margin-bottom:1.5rem;line-height:1.5;}

/* OPTIONS GRID */
.options-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px;}
.options-grid.cols3{grid-template-columns:1fr 1fr 1fr;}
.opt{border:1.5px solid var(--border);border-radius:14px;padding:1rem;cursor:pointer;transition:all 0.2s;text-align:left;background:var(--white);font-family:'Plus Jakarta Sans',sans-serif;}
.opt:hover{border-color:var(--blue-mid);background:var(--blue-light);}
.opt.selected{border-color:var(--blue);background:var(--blue-light);}
.opt-icon{font-size:24px;margin-bottom:0.5rem;display:block;}
.opt-title{font-size:13px;font-weight:600;color:var(--dark);margin-bottom:2px;}
.opt-desc{font-size:11px;color:var(--gray);line-height:1.4;}
.opt.selected .opt-title{color:var(--blue);}

/* CHECKBOX OPTIONS */
.check-grid{display:flex;flex-direction:column;gap:8px;}
.check-opt{display:flex;align-items:center;gap:12px;border:1.5px solid var(--border);border-radius:12px;padding:0.8rem 1rem;cursor:pointer;transition:all 0.2s;background:var(--white);}
.check-opt:hover{border-color:var(--blue-mid);}
.check-opt.selected{border-color:var(--blue);background:var(--blue-light);}
.check-box{width:20px;height:20px;border-radius:6px;border:1.5px solid var(--border);display:flex;align-items:center;justify-content:center;flex-shrink:0;transition:all 0.2s;}
.check-opt.selected .check-box{background:var(--blue);border-color:var(--blue);color:var(--white);font-size:12px;}
.check-label{font-size:13px;font-weight:500;}

/* INPUT */
.input-field{width:100%;border:1.5px solid var(--border);border-radius:12px;padding:0.8rem 1rem;font-size:14px;font-family:'Plus Jakarta Sans',sans-serif;color:var(--dark);outline:none;transition:border-color 0.2s;background:var(--white);}
.input-field:focus{border-color:var(--blue);}
.input-label{font-size:12px;font-weight:600;color:var(--dark);margin-bottom:0.4rem;display:block;}

/* BOUTONS NAV */
.nav-btns{display:flex;gap:10px;margin-top:1.5rem;}
.btn-next{flex:1;background:var(--blue);color:var(--white);border:none;padding:12px;border-radius:12px;font-size:14px;font-weight:700;cursor:pointer;font-family:'Plus Jakarta Sans',sans-serif;transition:background 0.2s;}
.btn-next:hover{background:#2F4AC0;}
.btn-next:disabled{background:var(--border);color:var(--gray);cursor:not-allowed;}
.btn-prev{background:var(--white);color:var(--dark);border:1.5px solid var(--border);padding:12px 20px;border-radius:12px;font-size:14px;font-weight:600;cursor:pointer;font-family:'Plus Jakarta Sans',sans-serif;transition:all 0.2s;}
.btn-prev:hover{border-color:var(--blue);color:var(--blue);}

/* RESULTS */
.results-wrap{max-width:600px;margin:0 auto;padding:1.5rem;display:none;}
.results-header{background:var(--white);border-radius:20px;padding:1.5rem 2rem;border:0.5px solid var(--border);margin-bottom:1.2rem;text-align:center;}
.results-emoji{font-size:40px;margin-bottom:0.8rem;}
.results-title{font-size:22px;font-weight:800;letter-spacing:-0.5px;margin-bottom:0.4rem;}
.results-sub{font-size:13px;color:var(--gray);}

.result-card{background:var(--white);border-radius:16px;padding:1.5rem;border:0.5px solid var(--border);margin-bottom:12px;animation:fadeIn 0.4s ease;}
.result-card-header{display:flex;align-items:start;gap:12px;margin-bottom:1rem;}
.result-rank{width:32px;height:32px;border-radius:10px;display:flex;align-items:center;justify-content:center;font-size:14px;font-weight:800;flex-shrink:0;}
.rank-1{background:var(--orange-light);color:var(--orange);}
.rank-2{background:var(--blue-light);color:var(--blue);}
.rank-3{background:var(--green-light);color:var(--green);}
.rank-other{background:#F3F4F6;color:var(--gray);}
.result-name{font-size:16px;font-weight:700;letter-spacing:-0.3px;margin-bottom:3px;}
.result-type{font-size:11px;font-weight:600;padding:3px 9px;border-radius:8px;display:inline-block;margin-bottom:0.4rem;}
.type-licence{background:var(--blue-light);color:var(--blue);}
.type-master{background:var(--purple-light);color:var(--purple);}
.type-BUT{background:var(--green-light);color:var(--green);}
.type-bachelor{background:var(--orange-light);color:var(--orange);}
.result-desc{font-size:12px;color:var(--gray);line-height:1.6;margin-bottom:1rem;}
.result-section-title{font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:1px;color:var(--gray);margin-bottom:0.6rem;}
.univs-list{display:flex;flex-direction:column;gap:6px;}
.univ-item{display:flex;align-items:center;gap:8px;padding:8px 12px;background:#F9FAFB;border-radius:10px;border:0.5px solid var(--border);}
.univ-dot{width:6px;height:6px;border-radius:50%;background:var(--blue);flex-shrink:0;}
.univ-name{font-size:12px;font-weight:500;}
.univ-city{font-size:11px;color:var(--gray);margin-left:auto;}
.result-tags{display:flex;gap:6px;flex-wrap:wrap;margin-top:0.8rem;}
.rtag{font-size:10px;padding:3px 9px;border-radius:8px;background:#F3F4F6;color:var(--gray);font-weight:500;}

.loading-card{background:var(--white);border-radius:20px;padding:3rem 2rem;border:0.5px solid var(--border);text-align:center;}
.loading-spinner{width:40px;height:40px;border:3px solid var(--border);border-top-color:var(--blue);border-radius:50%;animation:spin 0.8s linear infinite;margin:0 auto 1rem;}
@keyframes spin{to{transform:rotate(360deg)}}
.loading-text{font-size:14px;color:var(--gray);}

.btn-restart{width:100%;background:var(--white);color:var(--blue);border:1.5px solid var(--blue);padding:12px;border-radius:12px;font-size:14px;font-weight:700;cursor:pointer;font-family:'Plus Jakarta Sans',sans-serif;margin-top:0.5rem;transition:all 0.2s;}
.btn-restart:hover{background:var(--blue-light);}

footer{text-align:center;padding:2rem 1rem;font-size:12px;color:var(--gray);}
footer a{color:var(--blue);text-decoration:none;font-weight:600;}
</style>
</head>
<body>

<header>
  <div class="logo">
    <div class="logo-icon">O</div>
    <div class="logo-name">Orienta<span>Guide</span></div>
  </div>
  <div class="header-badge">🤖 Orienté par IA</div>
</header>

<div class="hero">
  <div class="hero-tag">✦ Outil d'orientation étudiante</div>
  <h1>Trouve ta formation<br><span>idéale</span> en 2 minutes</h1>
  <p>Réponds à quelques questions et notre IA t'oriente vers les formations et universités qui correspondent le mieux à ton profil.</p>
</div>

<div class="progress-wrap">
  <div class="progress-steps">
    <div class="pstep active" id="ps1"></div>
    <div class="pstep" id="ps2"></div>
    <div class="pstep" id="ps3"></div>
    <div class="pstep" id="ps4"></div>
    <div class="pstep" id="ps5"></div>
  </div>
  <div class="progress-label" id="progress-label">Étape 1 sur 5</div>
</div>

<div class="form-wrap">

  <!-- STEP 1 : Prénom -->
  <div class="step-card active" id="step1">
    <div class="step-eyebrow">Bienvenue 👋</div>
    <div class="step-title">Comment tu t'appelles ?</div>
    <div class="step-sub">Pour personnaliser tes recommandations.</div>
    <label class="input-label">Ton prénom</label>
    <input class="input-field" id="prenom" type="text" placeholder="Ex : Sophie" oninput="checkStep1()">
    <div class="nav-btns">
      <button class="btn-next" id="btn-next1" onclick="goTo(2)" disabled>Continuer →</button>
    </div>
  </div>

  <!-- STEP 2 : Niveau actuel -->
  <div class="step-card" id="step2">
    <div class="step-eyebrow">Ton profil</div>
    <div class="step-title">Tu es actuellement en quelle année ?</div>
    <div class="step-sub">Cela nous aide à te proposer les bonnes formations.</div>
    <div class="options-grid">
      <button class="opt" onclick="selectOpt(this,'niveau','Terminale')">
        <span class="opt-icon">🎒</span>
        <div class="opt-title">Terminale</div>
        <div class="opt-desc">Je cherche une formation post-bac</div>
      </button>
      <button class="opt" onclick="selectOpt(this,'niveau','Bac+1 / Bac+2')">
        <span class="opt-icon">📚</span>
        <div class="opt-title">Bac+1 / Bac+2</div>
        <div class="opt-desc">Je veux changer ou continuer</div>
      </button>
      <button class="opt" onclick="selectOpt(this,'niveau','Bac+3 Licence')">
        <span class="opt-icon">🎓</span>
        <div class="opt-title">Bac+3 / Licence</div>
        <div class="opt-desc">Je cherche un Master</div>
      </button>
      <button class="opt" onclick="selectOpt(this,'niveau','Autre')">
        <span class="opt-icon">🔄</span>
        <div class="opt-title">Reconversion</div>
        <div class="opt-desc">Je reprends mes études</div>
      </button>
    </div>
    <div class="nav-btns">
      <button class="btn-prev" onclick="goTo(1)">← Retour</button>
      <button class="btn-next" id="btn-next2" onclick="goTo(3)" disabled>Continuer →</button>
    </div>
  </div>

  <!-- STEP 3 : Type de bac -->
  <div class="step-card" id="step3">
    <div class="step-eyebrow">Ton bac</div>
    <div class="step-title">Tu as (ou tu prépares) quel bac ?</div>
    <div class="step-sub">Le type de bac influence les formations accessibles.</div>
    <div class="options-grid cols3">
      <button class="opt" onclick="selectOpt(this,'bac','Général')">
        <span class="opt-icon">📖</span>
        <div class="opt-title">Général</div>
      </button>
      <button class="opt" onclick="selectOpt(this,'bac','Technologique')">
        <span class="opt-icon">⚙️</span>
        <div class="opt-title">Techno</div>
      </button>
      <button class="opt" onclick="selectOpt(this,'bac','Professionnel')">
        <span class="opt-icon">🔧</span>
        <div class="opt-title">Pro</div>
      </button>
    </div>
    <div class="nav-btns">
      <button class="btn-prev" onclick="goTo(2)">← Retour</button>
      <button class="btn-next" id="btn-next3" onclick="goTo(4)" disabled>Continuer →</button>
    </div>
  </div>

  <!-- STEP 4 : Secteurs -->
  <div class="step-card" id="step4">
    <div class="step-eyebrow">Tes intérêts</div>
    <div class="step-title">Quels secteurs t'intéressent ?</div>
    <div class="step-sub">Tu peux en choisir plusieurs.</div>
    <div class="check-grid" id="secteurs-grid">
      <button class="check-opt" onclick="toggleCheck(this,'secteur','Communication & médias')">
        <div class="check-box"></div>
        <span class="check-label">🎙️ Communication & médias</span>
      </button>
      <button class="check-opt" onclick="toggleCheck(this,'secteur','Numérique & tech')">
        <div class="check-box"></div>
        <span class="check-label">💻 Numérique & tech</span>
      </button>
      <button class="check-opt" onclick="toggleCheck(this,'secteur','Sciences humaines & sociales')">
        <div class="check-box"></div>
        <span class="check-label">🧠 Sciences humaines & sociales</span>
      </button>
      <button class="check-opt" onclick="toggleCheck(this,'secteur','Commerce & gestion')">
        <div class="check-box"></div>
        <span class="check-label">📊 Commerce & gestion</span>
      </button>
      <button class="check-opt" onclick="toggleCheck(this,'secteur','Santé & social')">
        <div class="check-box"></div>
        <span class="check-label">🏥 Santé & social</span>
      </button>
      <button class="check-opt" onclick="toggleCheck(this,'secteur','Arts & design')">
        <div class="check-box"></div>
        <span class="check-label">🎨 Arts & design</span>
      </button>
      <button class="check-opt" onclick="toggleCheck(this,'secteur','Droit & sciences politiques')">
        <div class="check-box"></div>
        <span class="check-label">⚖️ Droit & sciences politiques</span>
      </button>
      <button class="check-opt" onclick="toggleCheck(this,'secteur','Sciences & ingénierie')">
        <div class="check-box"></div>
        <span class="check-label">🔬 Sciences & ingénierie</span>
      </button>
    </div>
    <div class="nav-btns">
      <button class="btn-prev" onclick="goTo(3)">← Retour</button>
      <button class="btn-next" id="btn-next4" onclick="goTo(5)" disabled>Continuer →</button>
    </div>
  </div>

  <!-- STEP 5 : Infos complémentaires -->
  <div class="step-card" id="step5">
    <div class="step-eyebrow">Dernière étape ✨</div>
    <div class="step-title">Quelques détails pour affiner</div>
    <div class="step-sub">Plus on en sait, mieux on t'oriente.</div>

    <label class="input-label" style="margin-top:0.5rem;">Tu vises quel niveau de formation ?</label>
    <div class="options-grid" style="margin-bottom:1rem;">
      <button class="opt" onclick="selectOpt(this,'niveau_vise','Licence / BUT (Bac+3)')">
        <span class="opt-icon">🎓</span>
        <div class="opt-title">Licence / BUT</div>
        <div class="opt-desc">Bac+3</div>
      </button>
      <button class="opt" onclick="selectOpt(this,'niveau_vise','Master (Bac+5)')">
        <span class="opt-icon">🏆</span>
        <div class="opt-title">Master</div>
        <div class="opt-desc">Bac+5</div>
      </button>
      <button class="opt" onclick="selectOpt(this,'niveau_vise','BTS / DUT (Bac+2)')">
        <span class="opt-icon">📋</span>
        <div class="opt-title">BTS / DUT</div>
        <div class="opt-desc">Bac+2</div>
      </button>
      <button class="opt" onclick="selectOpt(this,'niveau_vise','Pas encore décidé')">
        <span class="opt-icon">🤷</span>
        <div class="opt-title">Pas décidé</div>
        <div class="opt-desc">L'IA m'aide à choisir</div>
      </button>
    </div>

    <label class="input-label">Un mot sur toi ou ce que tu cherches vraiment <span style="color:var(--gray);font-weight:400;">(optionnel)</span></label>
    <textarea class="input-field" id="extra" rows="3" placeholder="Ex : j'aime créer des contenus, je veux travailler dans la formation, j'hésite entre deux voies..." style="resize:none;"></textarea>

    <div class="nav-btns">
      <button class="btn-prev" onclick="goTo(4)">← Retour</button>
      <button class="btn-next" id="btn-next5" onclick="submitForm()" disabled>Obtenir mes recommandations 🎯</button>
    </div>
  </div>

</div>

<!-- RESULTS -->
<div class="results-wrap" id="results-wrap">
  <div id="results-content"></div>
  <button class="btn-restart" onclick="restart()">↺ Recommencer pour un autre étudiant</button>
</div>

<footer>
  Outil conçu par <a href="#">Yanis Belbachir</a> · Projet associatif d'orientation étudiante · Propulsé par IA
</footer>

<script>
const data = {
  prenom: '',
  niveau: '',
  bac: '',
  secteurs: [],
  niveau_vise: '',
  extra: ''
};

function checkStep1() {
  const val = document.getElementById('prenom').value.trim();
  data.prenom = val;
  document.getElementById('btn-next1').disabled = val.length < 2;
}

function selectOpt(btn, key, val) {
  const parent = btn.closest('.options-grid, .step-card');
  parent.querySelectorAll('.opt').forEach(o => o.classList.remove('selected'));
  btn.classList.add('selected');
  data[key] = val;
  const step = btn.closest('.step-card');
  if (step) {
    const stepId = step.id;
    const num = stepId.replace('step','');
    const nextBtn = document.getElementById('btn-next'+num);
    if(nextBtn) nextBtn.disabled = false;
  }
}

function toggleCheck(btn, key, val) {
  btn.classList.toggle('selected');
  btn.querySelector('.check-box').textContent = btn.classList.contains('selected') ? '✓' : '';
  if(btn.classList.contains('selected')) {
    if(!data.secteurs.includes(val)) data.secteurs.push(val);
  } else {
    data.secteurs = data.secteurs.filter(s => s !== val);
  }
  document.getElementById('btn-next4').disabled = data.secteurs.length === 0;
}

function goTo(step) {
  document.querySelectorAll('.step-card').forEach(s => s.classList.remove('active'));
  document.getElementById('step'+step).classList.add('active');
  updateProgress(step);
  window.scrollTo({top:0,behavior:'smooth'});
}

function updateProgress(step) {
  for(let i=1;i<=5;i++) {
    const el = document.getElementById('ps'+i);
    el.className = 'pstep';
    if(i < step) el.classList.add('done');
    else if(i === step) el.classList.add('active');
  }
  document.getElementById('progress-label').textContent = `Étape ${step} sur 5`;
}

async function submitForm() {
  data.extra = document.getElementById('extra').value.trim();
  document.querySelector('.form-wrap').style.display = 'none';
  document.querySelector('.progress-wrap').style.display = 'none';
  document.querySelector('.hero').style.display = 'none';

  const rw = document.getElementById('results-wrap');
  rw.style.display = 'block';
  document.getElementById('results-content').innerHTML = `
    <div class="loading-card">
      <div class="loading-spinner"></div>
      <div class="loading-text">Notre IA analyse ton profil, <strong>${data.prenom}</strong>...<br>Quelques secondes ✨</div>
    </div>`;

  const prompt = `Tu es un conseiller d'orientation expert pour étudiants français. 
Voici le profil de l'étudiant :
- Prénom : ${data.prenom}
- Niveau actuel : ${data.niveau}
- Type de bac : ${data.bac}
- Secteurs d'intérêt : ${data.secteurs.join(', ')}
- Niveau de formation visé : ${data.niveau_vise}
- Informations supplémentaires : ${data.extra || 'Aucune'}

Génère 4 recommandations de formations adaptées à ce profil. 
Réponds UNIQUEMENT en JSON valide, sans markdown, sans backticks, ce format exact :
{
  "message": "Message personnalisé d'encouragement pour ${data.prenom} en 1-2 phrases",
  "formations": [
    {
      "nom": "Nom exact de la formation",
      "type": "Licence" ou "Master" ou "BUT" ou "BTS" ou "Bachelor",
      "description": "Description en 2 phrases de ce que prépare cette formation et ses débouchés",
      "pourquoi": "Pourquoi cette formation correspond à ce profil spécifique en 1 phrase",
      "universites": [
        {"nom": "Nom université", "ville": "Ville"},
        {"nom": "Nom université 2", "ville": "Ville"},
        {"nom": "Nom université 3", "ville": "Ville"}
      ],
      "tags": ["tag1", "tag2", "tag3"]
    }
  ]
}`;

  try {
    const response = await fetch("https://api.anthropic.com/v1/messages", {
      method: "POST",
      headers: {"Content-Type": "application/json"},
      body: JSON.stringify({
        model: "claude-sonnet-4-6",
        max_tokens: 1500,
        messages: [{role:"user", content: prompt}]
      })
    });
    const res = await response.json();
    const text = res.content[0].text.replace(/```json|```/g,'').trim();
    const json = JSON.parse(text);
    renderResults(json);
  } catch(e) {
    document.getElementById('results-content').innerHTML = `
      <div class="loading-card">
        <div style="font-size:40px;margin-bottom:1rem;">😔</div>
        <div class="loading-text">Une erreur est survenue. Réessaie dans quelques instants.</div>
      </div>`;
  }
}

function renderResults(data_res) {
  const ranks = ['rank-1','rank-2','rank-3','rank-other'];
  const rankEmoji = ['🥇','🥈','🥉','✦'];
  let html = `
    <div class="results-header">
      <div class="results-emoji">🎯</div>
      <div class="results-title">Tes recommandations, ${data.prenom} !</div>
      <div class="results-sub">${data_res.message}</div>
    </div>`;

  data_res.formations.forEach((f, i) => {
    const typeClass = 'type-'+f.type.toLowerCase().replace(/\s/g,'-').replace(/[^a-z-]/g,'');
    const univsHtml = f.universites.map(u => `
      <div class="univ-item">
        <div class="univ-dot"></div>
        <span class="univ-name">${u.nom}</span>
        <span class="univ-city">${u.ville}</span>
      </div>`).join('');
    const tagsHtml = f.tags.map(t => `<span class="rtag">${t}</span>`).join('');
    html += `
      <div class="result-card">
        <div class="result-card-header">
          <div class="result-rank ${ranks[i] || 'rank-other'}">${rankEmoji[i] || '✦'}</div>
          <div>
            <div class="result-name">${f.nom}</div>
            <span class="result-type ${typeClass}">${f.type}</span>
          </div>
        </div>
        <p class="result-desc">${f.description}</p>
        <p style="font-size:12px;color:var(--blue);font-weight:600;margin-bottom:1rem;">💡 ${f.pourquoi}</p>
        <div class="result-section-title">Universités qui proposent cette formation</div>
        <div class="univs-list">${univsHtml}</div>
        <div class="result-tags">${tagsHtml}</div>
      </div>`;
  });

  document.getElementById('results-content').innerHTML = html;
  window.scrollTo({top:0,behavior:'smooth'});
}

function restart() {
  data.prenom=''; data.niveau=''; data.bac=''; data.secteurs=[]; data.niveau_vise=''; data.extra='';
  document.querySelectorAll('.opt').forEach(o => o.classList.remove('selected'));
  document.querySelectorAll('.check-opt').forEach(o => {
    o.classList.remove('selected');
    o.querySelector('.check-box').textContent = '';
  });
  document.getElementById('prenom').value = '';
  document.getElementById('extra').value = '';
  document.querySelectorAll('.btn-next').forEach(b => b.disabled = true);
  document.getElementById('results-wrap').style.display = 'none';
  document.querySelector('.form-wrap').style.display = 'block';
  document.querySelector('.progress-wrap').style.display = 'block';
  document.querySelector('.hero').style.display = 'block';
  goTo(1);
}

// Step 5 validation
document.addEventListener('change', () => {
  if(data.niveau_vise) document.getElementById('btn-next5').disabled = false;
});
document.addEventListener('click', () => {
  setTimeout(() => {
    if(data.niveau_vise) document.getElementById('btn-next5').disabled = false;
  }, 100);
});
</script>
</body>
</html>
